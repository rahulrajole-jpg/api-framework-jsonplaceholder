[pytest]
addopts = --maxfail=1 --disable-warnings --alluredir=allure-results
markers =
    smoke: smoke tests
    regression: regression tests
    contract: schema/contract validation
    crossapi: cross-API validation
    db: database validation
    negative: negative/edge case testing
